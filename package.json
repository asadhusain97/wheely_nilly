{
  "name": "wheely-nilly",
  "version": "0.2.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE.md",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test frontend/tests/*.test.ts tests/api/*.test.ts",
    "test:all": "npm test && npm --prefix backend test && PYTHONPATH=sidecar sidecar/.venv/bin/pytest -q sidecar/tests"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "tsx": "^4.20.5",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "zod": "^4.4.3"
  }
}
